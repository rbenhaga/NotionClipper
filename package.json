{
  "name": "notion-clipper-monorepo",
  "version": "3.0.0",
  "description": "Notion Clipper Pro - Monorepo workspace",
  "private": true,
  "scripts": {
    "build:packages": "pnpm --filter @notion-clipper/i18n build && pnpm --filter @notion-clipper/notion-parser build && pnpm --filter \"@notion-clipper/core-*\" build && pnpm --filter \"@notion-clipper/adapters-*\" build && pnpm --filter @notion-clipper/ui build",
    "dev:app": "pnpm --filter @notion-clipper/app dev",
    "dev:extension": "pnpm --filter @notion-clipper/extension dev",
    "build:ui": "pnpm --filter @notion-clipper/ui build",
    "build:app": "pnpm build:packages && pnpm --filter @notion-clipper/app build",
    "build:extension": "pnpm build:packages && pnpm --filter @notion-clipper/extension build",
    "clean": "pnpm -r clean",
    "clean:dist": "rimraf packages/core-shared/dist packages/core-electron/dist packages/core-web/dist packages/adapters/electron/dist packages/adapters/webextension/dist packages/ui/dist apps/notion-clipper-app/dist apps/notion-clipper-extension/dist",
    "clean:dts": "rimraf packages/core-shared/dist packages/core-electron/dist packages/core-web/dist packages/adapters/electron/dist packages/adapters/webextension/dist packages/ui/dist",
    "clean:all": "pnpm clean:dist && rimraf node_modules",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "test": "pnpm -r test"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/adapters/*"
  ],
  "author": "Rayane Ben Haga",
  "license": "UNLICENSED",
  "devDependencies": {
    "@supabase/supabase-js": "^2.81.1",
    "@types/webextension-polyfill": "^0.12.4",
    "@vitejs/plugin-react": "^4.7.0",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "dotenv": "^17.2.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "rimraf": "^5.0.10",
    "sharp": "^0.33.5",
    "stripe": "^19.3.0",
    "typescript": "^5.0.0",
    "vite": "^5.4.20"
  },
  "dependencies": {
    "webextension-polyfill": "^0.12.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}